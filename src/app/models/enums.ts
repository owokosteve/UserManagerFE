export enum Reaction {
    Neutral,
    Like,
    Dislike,
}

export enum Role {
  Customer,
  Architect,
  Developer,
  Tester,
}

export enum ToastType {
    Success= "Success",
    Warning = "Warning",
    Error = "Error",
    Information = "Information",
}


export enum Status
{
    InProgress = 0,
    Active = 1,
    Inactive = 2,
    Deleted = 3,
    Created = 4,
}

export enum ControllerName{
  Documents = "Docs",
}
